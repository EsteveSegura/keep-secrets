{
  "name": "keep-secrets",
  "version": "0.1.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts" : {
    "start": "concurrently \"yarn workspace secrets-api start\" \"yarn workspace gateway dev\"",
    "dev": "concurrently \"yarn workspace gateway dev\" \"yarn workspace secrets-api dev\" \"yarn workspace frontend dev\""
  },
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}
