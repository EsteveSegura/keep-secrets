{
  "name": "keep-secrets",
  "version": "0.1.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts" : {
    "prestart": "concurrently \"yarn workspace frontend build\" ",
    "start": "concurrently \"yarn workspace secrets-api start\" \"yarn workspace gateway start\" \"yarn workspace frontend serve:build\"",
    "dev": "concurrently \"yarn workspace gateway dev\" \"yarn workspace secrets-api dev\" \"yarn workspace frontend dev --host\" \"yarn workspace docs dev\" \"yarn workspace auth-api dev\" \"yarn workspace testn dev\"",
    "lint:fix": "yarn workspace auth-api lint -- --fix && yarn workspace secrets-api lint -- --fix",
    "lint": "yarn workspace auth-api lint && yarn workspace secrets-api lint",
    "test": "yarn workspace auth-api test && yarn workspace secrets-api test"
  },
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}
